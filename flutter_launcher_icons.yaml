flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  
  # 👇 We use the file that has space around the logo
  image_path: "assets/images/logo_white.png" 
  min_sdk_android: 21
  
  # 👇 Background color stays black
  adaptive_icon_background: "#0D0D0D"
  
  # 👇 Foreground uses the smaller, centered logo
  adaptive_icon_foreground: "assets/images/logo_white.png"
  
  desaturate_tinted_to_grayscale_ios: true
  remove_alpha_ios: true